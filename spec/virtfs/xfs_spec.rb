require 'spec_helper'

describe Virtfs::Xfs do
  it 'has a version number' do
    expect(Virtfs::Xfs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
